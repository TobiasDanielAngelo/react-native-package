import { MyTrendChartProps } from ".";
export declare const MyLineChart: (<T extends Record<string, any>>({ data, traceKey, xKey, yKey, yPrefix, ySuffix, itemMap, formatter, excludedFromTotal, selectionLabel, noTotal, title, }: MyTrendChartProps<T>) => import("react/jsx-runtime").JSX.Element) & {
    displayName: string;
};
