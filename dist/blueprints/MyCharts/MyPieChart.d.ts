import { MyCircleChartProps } from ".";
export declare const MyPieChart: (<T extends Record<string, any>>({ data, colors, dataKey, nameKey, traceKey, itemMap, formatter, selectionLabel, title, }: MyCircleChartProps<T>) => import("react/jsx-runtime").JSX.Element) & {
    displayName: string;
};
